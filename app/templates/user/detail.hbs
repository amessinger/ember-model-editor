{{outlet}}

<h1>User "{{model.username}}" detail</h1>

<dl>
  <dt>Username</dt>
  <dd>{{model.username}}</dd>
  <dt>Name</dt>
  <dd>{{model.name}}</dd>
  <dt>Email</dt>
  <dd>{{model.email}}</dd>
</dl>

{{model-editor model=model}}
